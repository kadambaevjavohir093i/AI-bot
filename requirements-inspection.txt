# Dependencies for the inspection web app only (used by the Dockerfile).
# Kept separate from the root requirements.txt so the deployed image does not
# pull in the unrelated voice-bot stack (twilio/anthropic/telegram).
fastapi==0.115.6
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.12
httpx==0.27.2
python-dotenv==1.0.1
reportlab==4.2.5
Pillow==11.0.0
google-api-python-client==2.151.0
google-auth==2.36.0
